// Std interface internal

use::(
    "github.com/CandyCrafts/DevKitPlugin" -> dk,
)

pub let (
    flag1: dk::type::string,
)

pub impl (
    v: dk::attr(min: dk::type::ctn, max: dk::type::ctn);
    v -> email: dk::attr(host: []dk::type::string, IsWhiteList: dk::type::bool = dk::type::bool(true))
)